% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getAnnotation.R
\name{.manage_cache}
\alias{.manage_cache}
\title{Manage Caching and Offline Fallback}
\usage{
.manage_cache(
  mapping_file,
  log_file,
  remote_stamp,
  sub_dir = NULL,
  force_update = FALSE,
  exact_version = FALSE,
  offline = getOption("geneRosetta.offline", FALSE),
  bundled_root = .bundled_extdata_root()
)
}
\arguments{
\item{mapping_file}{Character string path to the cached mapping TSV file.}

\item{log_file}{Character string path to the JSON log file containing metadata.}

\item{remote_stamp}{Date/Character representing the remote data's version/date.}

\item{sub_dir}{Character string specifying the subdirectory within \code{inst/extdata}
where bundled fallback data resides.}

\item{force_update}{Logical; if \code{TRUE}, bypasses the local cache and forces a re-download.}

\item{exact_version}{Logical; if \code{TRUE}, requires exact equality between cached and remote version.}

\item{offline}{Logical; if \code{TRUE}, forces offline mode and uses cached/bundled data.}

\item{bundled_root}{Path to root extdata directory.}
}
\value{
A list with two elements:
\item{data}{A dataframe (from local cache or bundled internal data)
or NULL if a re-download is required.}
\item{download}{Logical; \code{TRUE} if the server is reachable and
the remote data is newer than the cache, \code{FALSE} otherwise.}
}
\description{
Checks if cached data is available and valid by comparing local metadata against
a remote timestamp. Implements a three-tier priority logic:
\enumerate{
\item Remote: If online and cache is stale, signals a re-download.
\item Local Cache: If offline or cache is fresh, returns user-downloaded data.
\item Bundled Data: If no local cache exists and offline, returns internal package data.
}
}
\keyword{internal}
