# Default Root Directory for Bundled Extdata

Default Root Directory for Bundled Extdata

## Usage

``` r
.bundled_extdata_root(override = NULL)
```

## Arguments

- override:

  Optional directory path for tests or fixtures.

## Value

Character string path to the bundled extdata root directory.
